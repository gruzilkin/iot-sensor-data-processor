version: '3.8'

services:
  queue:
    build: ./rabbitmq
    ports:
      - 15672:15672
      - 1883:1883
      - 5672:5672
    volumes:
      - ./rabbitmq/rabbitmq.config.default:/etc/rabbitmq/rabbitmq.config

  db:
    build: ./db
    ports:
      - "5432:5432"
    env_file:
      - .env.default
    volumes:
      - pg_data:/var/lib/postgresql/data

  db_writer:
    build: ./db_writer
    ports:
      - "5678:5678"
    env_file:
      - .env.default
    environment:
      - RABBITMQ_HOST=queue
      - PYTHONUNBUFFERED=1
      - DB_HOST=db
    depends_on:
      - queue
      - db
    deploy:
      mode: global
      restart_policy:
        condition: on-failure

  web:
    build: ./web
    ports:
      - "80:80"
    env_file:
      - .env.default
    environment:
      - RABBITMQ_HOST=queue
      - DB_HOST=db
    depends_on:
      - queue
      - db

volumes:
  pg_data: